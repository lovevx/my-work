# 《勇者传说》按键说明书

## 一、基本操作（键盘）

| 按键 | 功能 |
| ---- | ---- |
| `A` | 向左移动 |
| `D` | 向右移动 |
| `K` | 跳跃 |
| `J` | 攻击 |
| `L` | 滑铲 |
| `E` | 交互（对话 / 触发机关等） |
| `Esc` | 暂停 / 继续 |

## 二、操作技巧说明

- **跳跃（K）**：按K可在站立或移动时起跳；可实现滑铲跳。
- **攻击（J）**：近战攻击，可在连招判定窗口内连续按J打出连击。
- **滑铲（L）**：冲刺 / 滑铲，用于快速位移或规避攻击。
- **交互（E）**：靠近可交互目标时按E。

## 三、界面与菜单操作

- **主菜单**：使用鼠标点击即可选中按钮（`新建游戏 / 继续游戏 / 退出游戏`）；移动鼠标悬停会自动聚焦按钮，键盘方向键也可切换。游戏会自动保存进度，存在存档时"继续游戏"按钮才可用。
- **暂停菜单**：按 `Esc` 呼出，选择 `RESUME` 继续，或通过按钮返回主菜单。再按 `Esc` 也可快速返回游戏。
- **游戏失败界面**：按任意键 / 鼠标点击继续——若存在存档则自动读档重来，否则返回主菜单。
- **通关结算界面**：按任意键 / 鼠标点击逐行查看结局文本，看完后返回主菜单。
- **虚拟移动摇杆**：游戏内置触摸屏（触屏 / 移动端）专用的移动按钮；在 `A` / `D` 键位按压的同时拖动手柄即可移动。

## 四、备注

- 以上键位对应游戏配置中的输入动作：`move_left / move_right / move_jump / attack / slid / inter / pause`，可在 Godot 项目设置的 Input Map 中自定义。
- 本说明基于游戏当前版本输入配置整理。
